icon: '&#xe616;'
tags:
  en:
    - button
    - form
  zh-CN:
    - 表单
    - 按钮
props:
  type:
    type: String
    default: default
    en:
    zh-CN: 按钮类型，可选值为 `default,primary,warn`
  disabled:
    type: Boolean
    default: false
    en: if disable the button
    zh-CN: 是否不可点击
  text:
    type: String
    default: ''
    en: button text, has the same function as default slot
    zh-CN: 按钮文字，同默认slot
  mini:
    type: Boolean
    default: false
    en: if use smaller style
    zh-CN: 是否为mini类型，即小尺寸的按钮
  plain:
    type: Boolean
    default: false
    en: if use plain styles which has no backgroundColor
    zh-CN: 是否是plain样式，没有背景色
  action-type:
    zh-CN: '`button`的type属性，默认为浏览器默认(submit)，可选为 `submit` `button` `reset`'

   //bob
  size:
      type: String
      default: 无
      en:
      zh-CN: 新增尺寸属性，可选值  small smaller small-block

  noradius:
      type: Boolean
      default: false
      en:
      zh-CN: 圆角是否为0，默认有圆角

  inline:
        type: Boolean
        default: false
        en:
        zh-CN: 是否是行内元素

  link:
      type: String
      default: ""
      en:
      zh-CN: 跳转链接




slots:
  default:
    en: button text
    zh-CN: 按钮文字
changes:
  v2.1.0-rc.46:
      zh-CN:
        - '[feature] 添加`less`变量 #896 @erguotou520'