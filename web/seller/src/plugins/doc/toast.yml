component:
  toast

about:
  toast插件

author:
  bob

调用方式：


this.$vux.toast.show('Hello');

//type默认值：success
//type可选值：warn、cancel、success、text
this.$vux.toast.show({
   text: 'Hello World',
   type:'warn'
  });



this.$vux.toast.show({
        text: 'Hello World',
        onShow () {
        console.log('Plugin: I\'m showing')
    },
    onHide () {
        console.log('Plugin: I\'m hiding')
        _this.show9 = false
    }
});
