component:
  allchecked

about:
  全选组件

author:
  ice-yy

data() {
  return {
    btntext            // btntext是底部蓝色按钮内的文字
    checketext        // 全选文字   默认‘全选’
    list: []         // 新增的列表数据
        
    is-link         // 列表右边有箭头
          }
},
            
 format='{"id":"id","title":"name","value":"value"}'           
                    // id是列表的id 
                    // title 是第一行文本 16px
                    // inlineDesc 是第二行文本 14px
                    // time  groupname 是第三行文本 12px
                    // value 是列表右边文字


methods: {
                // 滚动加载
                load(data) {
                    if(data) {
                        this.list = data;
                    }
                },
                submit(data) {
                    console.log(data);
                                                            得到的是一个  Object {ids: Array[2], list: Array[2]}
                }
},

   示例：
   <c-all-checked :list='list' :btntext='btntext' is-link  value="4人" :checketext='checketext' :format='{"id":"id","title":"name","value":"name"}' @submit="submit"></c-all-checked>
                    
   备注：
   is-link：Boollean
   btntext: String,
   checketext: String,
   isLink: Boolean,
   list: [Array],
   format: Object